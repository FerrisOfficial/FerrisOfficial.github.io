---
---

<section class="section about" id="about">
  <div class="container about-inner">
    <div class="about-text">
      <h2 class="about-heading">How I work</h2>

      <p class="about-body">
        I work from observable behaviour: reproduce the issue, narrow the failure, make the
        smallest safe change and verify it with tests and the system's existing tooling. I
        value clear ownership, explicit trade-offs and leaving the codebase easier to
        understand than I found it.
      </p>
    </div>
  </div>
</section>

<style>
  .about-inner {
    max-width: var(--measure);
  }

  .about-text {
    min-width: 0;
  }

  .about-heading {
    font-size: var(--text-2xl);
  }

  .about-body {
    max-width: var(--measure);
    margin-top: var(--space-4);
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--text-muted);
  }

</style>
