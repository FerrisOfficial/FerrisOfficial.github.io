---
---

<section class="section about" id="about">
  <div class="container about-inner">
    <div class="about-text">
      <p class="section-label">About</p>

      <h2 class="about-heading">How I work</h2>

      <p class="about-body">
        I moved into a production .NET role and quickly began contributing to backend APIs,
        KSeF integration and production debugging. I value C++ for low-level control and
        performance, and Python for engineering tools, simulation and ML experimentation.
        My AI-assisted workflow uses explicit plans, controlled repository context, testing
        and verification.
      </p>
    </div>
  </div>
</section>

<style>
  .about-inner {
    max-width: var(--measure);
  }

  .about-text {
    min-width: 0;
  }

  .about-heading {
    font-size: var(--text-2xl);
  }

  .about-body {
    max-width: var(--measure);
    margin-top: var(--space-4);
    font-size: var(--text-base);
    line-height: var(--leading-relaxed);
    color: var(--text-muted);
  }

</style>
